[project]
name = "friday"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "colorama>=0.4.6",
    "gradio>=6.6.0",
    "openai>=2.21.0",
    "python-dotenv>=1.2.1",
]

[dependency-groups]
dev = [
    "watchfiles>=1.1.1",
]
